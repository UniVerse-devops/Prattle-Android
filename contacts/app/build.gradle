plugins {
    id 'signal-sample-app'
}

android {
    namespace 'org.spark.contactstest'

    defaultConfig {
        applicationId "org.spark.contactstest"
    }
}

dependencies {
    implementation project(':contacts')
}